import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'av.lenguajes.moview',
  appName: 'Moview',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
